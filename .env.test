# Use a dedicated MongoDB database for tests so that running the test suite
# (tests/Api/AbstractFilmTestCase::clearDatabase drops collections on every test)
# never touches the "api" database used by the dev environment.
MONGODB_URI=mongodb://api-platform:!ChangeMe!@db-mongodb/api_test?authSource=admin
MONGODB_DB=api_test
